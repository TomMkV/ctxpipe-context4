---
source: linear
type: issue
id: 0192be25-33fd-48bc-bdc6-99dde3e56c27
identifier: CTX-31
title: Extend auth sessions
url: https://linear.app/ctxpipe/issue/CTX-31/extend-auth-sessions
state: Done
priority: Low
teamId: 080a5a17-fa66-45af-9009-35d46eed1fc9
projectId: 29b1bae6-2c19-4e9f-b8b0-4a7a2bd28b3e
cycleId: null
assigneeId: 4b501e62-2fb1-40f2-b918-10505ad31010
creatorId: 4b501e62-2fb1-40f2-b918-10505ad31010
labelIds:
  - 66a2e21f-a34f-4439-af5b-39e2a103e3f5
createdAt: 2026-04-13T23:42:03.400Z
updatedAt: 2026-04-15T11:23:29.935Z
githubReferences:
  - kind: pull_request
    url: https://github.com/ctxpipe-ai/ctxpipe/pull/84
    title: "fix(auth): extend sessions and MCP OAuth token lifetime (CTX-31)"
attachments: []
---

# CTX-31: Extend auth sessions

The session expires after (seemingly) a few hours of inactivity. Perhaps we could enabled 'remember me' + extend the session length, or similar, so we remove some friction for next day's session?

It feels a bit clunky to log into app.ctxpipe.ai every day. (Also, would this have any connection to the MCP flakiness with connection?)

## Comments

### 2026-04-15T11:23:23.484Z · bf21aa8a-4c82-447c-9af7-94b0e739ec50

closed in PR #100

### 2026-04-15T11:14:45.807Z · 8896c76e-47b1-481d-8912-6cd1cf17848d

[**Session and token configuration**](<https://www.cursor.com/agents/bc-0f8bbc6e-660d-4e96-a508-5887f6dbfb63>)

Restored the 4h MCP OAuth access token in `oauthProvider` and aligned docs.

`apps/backend/src/auth/config.ts`

* Re-added `accessTokenExpiresIn: 14_400` with the existing comment.
* Adjusted the `session` block comment so it doesn’t imply a 1h MCP token.

`apps/backend/AGENTS.md`

* MCP OAuth bullet again says access tokens are **4h** and that clients must refresh.

Pushed commit `ce1e5e9` on `cursor/extend-web-session-short-oauth-token-fb63` and updated PR #100. Auth tests (`withAuth` + `auth` routes) still pass.

**Repository:** `ctxpipe-ai/ctxpipe`
**Branch:** `cursor/extend-web-session-short-oauth-token-fb63`

[View PR](<https://github.com/ctxpipe-ai/ctxpipe/pull/100>) · [Open in Desktop](<https://cursor.com/background-agent?bcId=bc-0f8bbc6e-660d-4e96-a508-5887f6dbfb63>) · [Open in Web](<https://www.cursor.com/agents/bc-0f8bbc6e-660d-4e96-a508-5887f6dbfb63>)

### 2026-04-15T10:52:45.922Z · 8896c76e-47b1-481d-8912-6cd1cf17848d

[**Session and token configuration**](<https://www.cursor.com/agents/bc-0f8bbc6e-660d-4e96-a508-5887f6dbfb63>)

Here is what was implemented for the follow-up (“keep increased session time, return access token TTL”).

### Changes (PR [#100](<https://github.com/ctxpipe-ai/ctxpipe/pull/100>))

1. **Web session (browser + DB)** — explicit longer lifetime in `createBetterAuth()`:
   * `expiresIn`: **30 days**
   * `updateAge`: **1 day** (sliding refresh)
   * `storeSessionInDatabase: true` — required by `@better-auth/oauth-provider` when **secondary storage** is in use (from the infra `dash()` plugin); without this, Better Auth throws at runtime.
2. **OAuth MCP access tokens** — removed `accessTokenExpiresIn: 14_400`, so tokens use the library default **3600 seconds (1 hour)** again.
3. **Docs** — `apps/backend/AGENTS.md` now describes **1h** MCP OAuth access tokens (not 4h).
4. **Tests** — `withAuth.test.ts` mocks `auth.handler` with `Promise.resolve(...)` so `auth.handler(...).catch` in `resolveJwks` works (the real handler returns a Promise).

### Linear / product note

* **Web “stay signed in”** is driven by **session cookie + DB session** (`session.expiresIn` / `updateAge`).
* **MCP Bearer JWTs** are **OAuth access tokens** with their **own** expiry (now back to **1h**). Clients should **refresh** with the refresh token; that is separate from “logging into the website tomorrow.”

Verified with: `vitest run src/auth/withAuth.test.ts src/routes/auth.test.ts`.

**Repository:** `ctxpipe-ai/ctxpipe`
**Branch:** `cursor/extend-web-session-short-oauth-token-fb63`

[View PR](<https://github.com/ctxpipe-ai/ctxpipe/pull/100>) · [Open in Desktop](<https://cursor.com/background-agent?bcId=bc-0f8bbc6e-660d-4e96-a508-5887f6dbfb63>) · [Open in Web](<https://www.cursor.com/agents/bc-0f8bbc6e-660d-4e96-a508-5887f6dbfb63>)

### 2026-04-15T10:33:43.374Z · bf21aa8a-4c82-447c-9af7-94b0e739ec50

@cursor in the PR that was raised, keep the increased session time, but return back the accessToken TTL, we don't want to extend that

### 2026-04-15T10:29:49.446Z · 8896c76e-47b1-481d-8912-6cd1cf17848d

⚠️ For now, only the creator of this agent can send follow-ups.

### 2026-04-15T10:29:45.928Z · bf21aa8a-4c82-447c-9af7-94b0e739ec50

this ticket is focused only on web session which we can increase. But keep the accessToken short

### 2026-04-14T07:38:12.234Z · 4b501e62-2fb1-40f2-b918-10505ad31010

@jakub want to confirm this one? Could be tied to [CTX-13](https://linear.app/ctxpipe/issue/CTX-13/mcp-connection-issues)?

### 2026-04-14T07:27:20.001Z · 8896c76e-47b1-481d-8912-6cd1cf17848d

[**Auth session persistence**](<https://www.cursor.com/agents/bc-35099fa8-fa24-4763-b644-d71111555bf3>)

## What was going wrong

Two separate mechanisms were in play:

1. **Web app (cookies + DB session)** — Better Auth’s defaults are a **7-day** session with a **1-day** sliding refresh (`updateAge`). That alone does not explain “a few hours” unless something else was shortening perceived auth.
2. **MCP (**`/mcp` with `Authorization: Bearer`) — `withBearerAuth` verifies a **JWT** whose expiry comes from the **OAuth provider access token**, which defaults to `accessTokenExpiresIn` = 3600 seconds (1 hour) in `@better-auth/oauth-provider`. So MCP clients could look “flaky” or drop while the browser session still felt fine: the **bearer JWT expired hourly** even when the underlying session row lived longer.

So yes: **MCP flakiness can be tied to session/token lifetime**, but specifically to **OAuth access-token JWT lifetime**, not only “inactivity” on the web app.

## What I changed

In `apps/backend/src/auth/config.ts`:

* `session`: `expiresIn: 30 days`, `updateAge: 24 hours` — longer-lived browser sessions with a once-per-day sliding extension when the session is used.
* `oauthProvider`: `accessTokenExpiresIn: 7 days` — JWTs used for Bearer MCP auth align with a multi-day window instead of the previous **1-hour** default.

Draft PR: [https://github.com/ctxpipe-ai/ctxpipe/pull/84](<https://github.com/ctxpipe-ai/ctxpipe/pull/84>)

**Security caveat (worth stating plainly):** longer-lived bearer tokens increase blast radius if leaked. If your threat model prefers shorter MCP tokens, shorten `OAUTH_ACCESS_TOKEN_EXPIRES_IN_SECONDS` or implement refresh in the MCP client instead of stretching `exp` this far.

**“Remember me”:** Better Auth already supports persistent vs session-only cookies; the UI uses `@daveyplate/better-auth-ui`’s `AuthView`, which can expose that if enabled upstream. This change focuses on the **server-side TTLs** that were clearly misaligned (especially for MCP).

**Repository:** `ctxpipe-ai/ctxpipe`
**Branch:** `cursor/extend-auth-sessions-5bf3`

[View PR](<https://github.com/ctxpipe-ai/ctxpipe/pull/84>) · [Open in Desktop](<https://cursor.com/background-agent?bcId=bc-35099fa8-fa24-4763-b644-d71111555bf3>) · [Open in Web](<https://www.cursor.com/agents/bc-35099fa8-fa24-4763-b644-d71111555bf3>)

### 2026-04-14T07:16:28.430Z

This thread is for an agent session with Cursor. [View on Cursor →](https://www.cursor.com/agents/bc-35099fa8-fa24-4763-b644-d71111555bf3)
