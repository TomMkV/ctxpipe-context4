---
source: linear
type: issue
id: 99cc7eec-6fba-4326-b04a-8c8eb205559c
identifier: CTX-111
title: Fix deploy Forge App Github Action which is failing
url: https://linear.app/ctxpipe/issue/CTX-111/fix-deploy-forge-app-github-action-which-is-failing
state: Done
priority: No priority
teamId: 080a5a17-fa66-45af-9009-35d46eed1fc9
projectId: null
cycleId: null
assigneeId: b18de5ef-1f2c-42b9-8520-d767be2640a1
creatorId: b18de5ef-1f2c-42b9-8520-d767be2640a1
labelIds: []
createdAt: 2026-05-20T06:48:54.099Z
updatedAt: 2026-05-21T02:28:40.519Z
githubReferences:
  - kind: pull_request
    url: https://github.com/ctxpipe-ai/ctxpipe/pull/184
    title: "fix(ci): pass Forge environment to lint in deploy workflow"
attachments: []
---

# CTX-111: Fix deploy Forge App Github Action which is failing

[Example of the Deploy Forge App github action failing](<https://github.com/ctxpipe-ai/ctxpipe/actions/runs/26034271199/job/76528670736>)

## Comments

### 2026-05-20T10:03:08.536Z · 8896c76e-47b1-481d-8912-6cd1cf17848d

[**Deploy forge app github action**](<https://www.cursor.com/agents/bc-dd6ea773-c4e1-4990-b3c6-367db75fe143>)

## Review summary

I walked through the full `forge_deploy` job, compared it to `apps/backend/src/scripts/forge-provision-cli.mjs` (which already handles headless Forge), and simulated the CLI in a non-TTY environment. Besides the lint fix already in [PR #184](<https://github.com/ctxpipe-ai/ctxpipe/pull/184>), here is what can still block deployment.

---

### Fixed / hardened in PR #184

| Issue | Risk | Mitigation added |
| -- | -- | -- |
| `forge lint` without `--environment` | **Blocker** (observed in run 26034271199) | `lint:forge` uses `--environment production` |
| `forge deploy --non-interactive` without analytics setting | **Blocker** on fresh runners | `prepare-forge-cli` + workflow step (same pattern as provision worker) |

`forge deploy --non-interactive` fails with:

```text
Error: --non-interactive requires an analytics setting.
Use forge settings set usage-analytics <value>.
```

`FORGE_DISABLE_ANALYTICS=1` helps for some commands, but the worker explicitly runs `forge settings set usage-analytics false` — that is the reliable fix for deploy.

---

### Deploy step — looks OK if secrets are valid

```275:287:.github/workflows/deploy.yaml
      - name: Deploy to Forge production
        env:
          FORGE_EMAIL: ${{ secrets.FORGE_EMAIL }}
          FORGE_API_TOKEN: ${{ secrets.FORGE_API_TOKEN }}
          FORGE_DISABLE_ANALYTICS: "1"
        run: pnpm --filter @ctxpipe/forge-ctxpipe-agent run deploy:prod
```

`deploy:prod` already has the right shape:

* `--environment production --non-interactive`
* `REMOTE_BASE_URL=https://app.ctxpipe.ai/` (required for `${REMOTE_BASE_URL}` in `manifest.yml`)
* Runs from `apps/forge-ctxpipe-agent` via pnpm filter (manifest on disk)

After lint passes, deploy should reach Atlassian unless auth or permissions fail.

---

### Remaining risks (operational / design)

**1. Forge credentials (high likelihood if deploy ever ran)**

Both lint and deploy need headless auth:

* `FORGE_EMAIL` — Atlassian account email for the token owner
* `FORGE_API_TOKEN` — scoped Forge API token

Failures look like `Not logged in` or `401` / invalid token. These must live in the GitHub `production` environment secrets and match the account that owns app `4ce198e3-2ce7-4a6e-865f-a3e31d15fe43` in `manifest.yml`.

**2. Deploy permission (medium)**

The token user must be allowed to deploy that app. The backend maps this to `forge_deploy_forbidden`. Wrong account or insufficient Forge/Developer Console access will fail deploy even with valid credentials.

**3. No** `forge install` **in CI (by design, but worth knowing)**

CI only runs `forge deploy`, not `forge install --upgrade`. That matches a **shared reference/marketplace app** (`app.id` is fixed in `manifest.yml`). Per-tenant installs are handled by the **provision worker** (`forge-provision-cli.mjs`: deploy + install to `CONFLUENCE_SITE`).

So CI updates the app bundle; it does **not** push upgrades to every customer site. That is expected if product provision owns installs.

**4. Manifest drift vs backend generator (medium over time)**

`apps/forge-ctxpipe-agent/manifest.yml` must stay aligned with `apps/backend/src/lib/forge-app-manifest.ts` (modules, scopes, routes). The reference manifest uses `${REMOTE_BASE_URL}`; the worker inlines the public API URL. Drift would pass CI lint but break provision or runtime behavior.

**5. pnpm ignored** `@forge/cli` **build scripts (low)**

CI logs show `@forge/cli` postinstall scripts skipped. The `forge` binary still runs from `node_modules/.bin/forge` in testing. Worth watching if CLI behavior regresses after upgrades.

**6. Job ordering (low)**

`forge_deploy` `needs: terraform`. Terraform failures skip Forge entirely (deploy step never ran in the failing run you linked — only lint failed).

**7.** `install:upgrade` **script not CI-safe (low for this workflow)**

```18:18:apps/forge-ctxpipe-agent/package.json
    "install:upgrade": "REMOTE_BASE_URL=https://app.ctxpipe.ai/ forge install --upgrade",
```

Missing `--non-interactive`, `-e production`, and site/product flags. Fine that CI does not call it; risky if someone runs it manually in automation.

---

### What the last failed run tells us

Run [26034271199](<https://github.com/ctxpipe-ai/ctxpipe/actions/runs/26034271199>): **Deploy to Forge production was skipped** because lint failed first. So there is no production deploy log yet — the analytics + lint fixes are the right next step before you can validate deploy end-to-end.

---

### Recommendation after merge

1. Merge PR #184 and watch the next `main` deploy.
2. If deploy fails, check the new step logs in order: **Prepare Forge CLI** → **Forge lint** → **Deploy to Forge production**.
3. Confirm `FORGE_EMAIL` / `FORGE_API_TOKEN` in the `production` environment match the app owner in Developer Console.

I can add an optional post-deploy `forge install --upgrade` step for `ctxpipe.atlassian.net` if you want CI to mirror local `install:upgrade` — say if that is part of your intended production flow.

**Repository:** `ctxpipe-ai/ctxpipe`
**Branch:** `cursor-cloud-fix-forge-deploy-action-e143`

[View PR](<https://github.com/ctxpipe-ai/ctxpipe/pull/184>) · [Open in Desktop](<https://cursor.com/background-agent?bcId=bc-dd6ea773-c4e1-4990-b3c6-367db75fe143>) · [Open in Web](<https://www.cursor.com/agents/bc-dd6ea773-c4e1-4990-b3c6-367db75fe143>)

### 2026-05-20T10:00:36.295Z · b18de5ef-1f2c-42b9-8520-d767be2640a1

While looking at this error, coukd you review our forge deploy step to see if there is any other potential issue that can block the deployment?

### 2026-05-20T08:34:27.914Z · 8896c76e-47b1-481d-8912-6cd1cf17848d

[**Deploy forge app github action**](<https://www.cursor.com/agents/bc-dd6ea773-c4e1-4990-b3c6-367db75fe143>)

## Summary

Fixed **CTX-111** — the **Deploy Forge app (production)** job was failing at **Forge lint** because `forge lint` ran without an environment, so the Forge CLI tried to prompt for a default development environment in non-interactive CI:

```
You do not have a default development environment set...
Error: Prompts can not be meaningfully rendered in non-TTY environments
```

## Changes

1. `apps/forge-ctxpipe-agent/package.json` — added `lint:forge` script:
   * `forge lint --environment production` (same environment as `deploy:prod`)
2. `.github/workflows/deploy.yaml` — switched the lint step from `exec forge lint` to `run lint:forge`

The workflow already sets `REMOTE_BASE_URL`, `FORGE_EMAIL`, `FORGE_API_TOKEN`, and `FORGE_DISABLE_ANALYTICS` for that step.

## PR

Draft PR: [https://github.com/ctxpipe-ai/ctxpipe/pull/184](<https://github.com/ctxpipe-ai/ctxpipe/pull/184>)

After merge, the next deploy workflow run should get past the lint step (assuming Forge credentials in the `production` environment are still valid).

**Repository:** `ctxpipe-ai/ctxpipe`
**Branch:** `cursor-cloud-fix-forge-deploy-action-e143`

[View PR](<https://github.com/ctxpipe-ai/ctxpipe/pull/184>) · [Open in Desktop](<https://cursor.com/background-agent?bcId=bc-dd6ea773-c4e1-4990-b3c6-367db75fe143>) · [Open in Web](<https://www.cursor.com/agents/bc-dd6ea773-c4e1-4990-b3c6-367db75fe143>)

### 2026-05-20T08:32:18.392Z · b18de5ef-1f2c-42b9-8520-d767be2640a1

ctxpipe-ai/ctxpipe is the repo you should work on

### 2026-05-20T08:31:57.528Z · b18de5ef-1f2c-42b9-8520-d767be2640a1

Hey @cursor look at the error and open a PR to fix the issue please

### 2026-05-20T08:31:15.550Z · 8896c76e-47b1-481d-8912-6cd1cf17848d

Cloud Agent not running.: This Cloud Agent is no longer available.

Try again or contact Cursor support at [hi@cursor.com](<mailto:hi@cursor.com>).

**Request ID:** `serverGenReqId_e8ff2227-4066-4249-ba7a-ab1c7cbce503`

### 2026-05-20T08:18:50.149Z · 8896c76e-47b1-481d-8912-6cd1cf17848d

Cloud agent has stopped - send another message to resume.

### 2026-05-20T08:17:39.706Z · b18de5ef-1f2c-42b9-8520-d767be2640a1

You should work on ctxpipe-ai/ctxpipe repo

### 2026-05-20T06:48:55.968Z

This thread is for an agent session with Cursor. [View on Cursor →](https://www.cursor.com/agents/bc-879d21bb-38fd-44fc-b6b2-37911f22e158)
