---
source: linear
type: issue
id: 6224996e-99eb-4e87-b154-649e7990a6f4
identifier: CTX-16
title: CodeRabbit not authorising app
url: https://linear.app/ctxpipe/issue/CTX-16/coderabbit-not-authorising-app
state: Done
priority: High
teamId: 080a5a17-fa66-45af-9009-35d46eed1fc9
projectId: 29b1bae6-2c19-4e9f-b8b0-4a7a2bd28b3e
cycleId: null
assigneeId: bf21aa8a-4c82-447c-9af7-94b0e739ec50
creatorId: 4b501e62-2fb1-40f2-b918-10505ad31010
labelIds:
  - 81bb09c4-a8eb-41b2-ac0a-bd2168a227d4
  - 38eb1a27-dda7-4ecd-b431-95319f345f4d
createdAt: 2026-04-13T06:18:32.640Z
updatedAt: 2026-04-20T03:00:38.001Z
githubReferences:
  - kind: pull_request
    url: https://github.com/ctxpipe-ai/ctxpipe/pull/115
    title: "fix(auth): validate opaque MCP OAuth access tokens (CTX-16)"
attachments: []
---

# CTX-16: CodeRabbit not authorising app

Adding the MCP to Coderabbit gives this error after successfully triggering the OAuth screen in the correct org session:

```
Failed to complete OAuth authorization
Server requires authentication. Please enter your access token below.
```

Image:

![image.png](https://uploads.linear.app/59dca29b-5862-4ade-bd5e-78a7e57013d5/090c6587-41b5-4a4b-a49f-d79c883b965e/10839a7f-0515-4c21-a6e5-430cdc8e6498)

We have the MCP working in the IDE for both Cursor and Claude Code - so the MCPs work, we just need to verify and solve for Code Review tools like CodeRabbit, BugBot, Graphite and so on.

## Comments

### 2026-04-19T11:25:32.071Z · 8896c76e-47b1-481d-8912-6cd1cf17848d

[**Coderabbit authorization failure**](<https://www.cursor.com/agents/bc-6e159965-cb89-4a11-8f12-951939150ba6>)

Here is what is going on, without pretending CodeRabbit’s UI is identical in every build.

## What that message actually means

“Failed to complete OAuth authorisation … try entering your access token manually” is almost always **their generic recovery text**: from CodeRabbit’s point of view, the OAuth **handshake did not finish cleanly** (token exchange, storing tokens, or the **first authenticated call** to your MCP URL failed). The product then suggests the **manual** path as a fallback.

That is **not** the same as “you did something wrong by picking OAuth”. OAuth can “look” finished in the browser while **their server** still does not have a valid access token, or has one but **the next request** to your endpoint returns **401** and they map that to this message.

## What “access token” would be (if you ever had to paste one)

It would be the **OAuth access token** their client obtained for your MCP resource — in your stack, that is the **JWT** issued by your auth server for the MCP audience, **not** your GitHub token, not a Linear key, not an API key you create in the ctx| UI.

You are not meant to hunt that down in normal use: a proper MCP+OAuth integration should **store it after the code exchange** and send `Authorization: Bearer <that JWT>` on MCP requests. If their UI does not expose a field when OAuth is selected, that is **their UX gap** (or a bug where the manual path is referenced but not wired for MCP-only OAuth).

## Why there is no way to enter it

Typical reasons:

* **Manual token is tied to another auth mode** (e.g. “API key” or “Bearer token”) and the OAuth-only form **does not show** a paste box — so the copy is misleading.
* **OAuth failed before they persisted anything**, so they never enable the “advanced / manual” panel.

Either way: **you are not missing a secret “ctx| manual token”** — if OAuth completed on their side, you would not paste anything; if it did not, the fix is on **integration or server behaviour**, not on you inventing a token.

## What actually helps on your side

1. **After our backend fix is deployed** (PR that resolves bearer JWTs without `sid`): many “OAuth succeeded then connection failed” cases should **disappear**, because the first MCP call would stop getting **401** for otherwise valid tokens.
2. If it **still** fails after deploy, the next suspects are **issuer/audience mismatch** (`AUTH_BASE_URL` / `AUTH_ISSUER` vs the URL CodeRabbit calls) or **CodeRabbit not sending** `Authorization: Bearer` on MCP — that needs **their** support or logs, not a token you paste from nowhere.

**Bottom line:** the message is a **misleading fallback**; the real problem is “OAuth did not fully succeed from their connector’s perspective or the MCP call was rejected”. It is **not** asking you for a separate mystery token you should know — and if there is no input, their copy is simply poor. Your practical move is **retry after the server fix is live**; if it persists, **open a ticket with CodeRabbit** and ask what HTTP status they get on the post-OAuth MCP request and whether they support your OAuth metadata end-to-end.

**Repository:** `ctxpipe-ai/ctxpipe`
**Branch:** `cursor/coderabbit-mcp-oauth-bearer-sub-0ba6`

[View PR](<https://github.com/ctxpipe-ai/ctxpipe/pull/113>) · [Open in Desktop](<https://cursor.com/background-agent?bcId=bc-6e159965-cb89-4a11-8f12-951939150ba6>) · [Open in Web](<https://www.cursor.com/agents/bc-6e159965-cb89-4a11-8f12-951939150ba6>)

### 2026-04-19T11:25:13.246Z · 4b501e62-2fb1-40f2-b918-10505ad31010

I've used OAuth as the method, not API key or 'none'. The error message says 'Failt ro complete OAuth authorisation. Connection failed, Please try entering your access token manually' - howerver there is no method to enter this token - and I don't know what token it would be?

### 2026-04-19T11:22:00.743Z · 8896c76e-47b1-481d-8912-6cd1cf17848d

[**Coderabbit authorization failure**](<https://www.cursor.com/agents/bc-6e159965-cb89-4a11-8f12-951939150ba6>)

I couldn’t read the image bytes from that Linear URL (the fetch came back effectively empty), so I’m going off the behaviour you described: **browser OAuth finishes and shows success, then CodeRabbit’s own step fails**.

That pattern is still very informative:

**What it usually rules out**

* It is **not** “OAuth never started” or “user never approved” — the AS leg completed.
* It is **not** (primarily) a bad `orgSlug` in the MCP URL — that would bite on the **first** `/mcp` request as a **400** with our JSON-RPC error, not a generic OAuth-complete-then-fail story.

**What it strongly suggests**

1. **Failure is on the resource step, not the consent screen** — i.e. after tokens exist, CodeRabbit’s backend does something that fails: **token exchange**, **first MCP call**, or **refresh**. Your original “server requires authentication / access token” copy is exactly what many UIs show when the **next HTTP call to** `/mcp` returns 401 (or when they never got a usable access token to send).
2. **Our earlier root cause still fits**: access JWT verifies, but the app treated the request as **unauthenticated** because `sid` was missing and we never bound `user`/`session` — so `requireAuth` → 401 even though OAuth “succeeded”. The PR fix is aimed squarely at that.

**Other possibilities if it still fails after deploy**

* **Bearer never sent** on the MCP request (bug or limitation on their side).
* **JWT fails verification** on `/mcp`: wrong `aud`/`iss` relative to `AUTH_BASE_URL` / `AUTH_ISSUER` for the URL CodeRabbit actually calls.
* **Token exchange** failing after redirect (wrong `redirect_uri`, dynamic client registration quirks) — often a different error string than “access token”.

If you can paste the **exact error text** from that screenshot (or HTTP status if shown), we can narrow it to exchange vs first MCP vs JWT validation. Otherwise: **yes, “OAuth success then failure” is a hint that the problem is post-consent token or first authenticated MCP call — consistent with the missing-**`sid` bearer path we fixed, not with org slug.

**Repository:** `ctxpipe-ai/ctxpipe`
**Branch:** `cursor/coderabbit-mcp-oauth-bearer-sub-0ba6`

[View PR](<https://github.com/ctxpipe-ai/ctxpipe/pull/113>) · [Open in Desktop](<https://cursor.com/background-agent?bcId=bc-6e159965-cb89-4a11-8f12-951939150ba6>) · [Open in Web](<https://www.cursor.com/agents/bc-6e159965-cb89-4a11-8f12-951939150ba6>)

### 2026-04-19T11:13:59.119Z · 4b501e62-2fb1-40f2-b918-10505ad31010

![Screenshot 2026-04-19 at 9.12.43 PM.png](https://uploads.linear.app/59dca29b-5862-4ade-bd5e-78a7e57013d5/990af596-450d-40a3-a081-35ddaf049cc3/e1318454-1081-4cf3-b4b0-5e69b2353591)

This is the error CodeRabbit gives, after the OAuth model prompts me with confirmation of access and 'says 'success'. Goes this give any hint on the root cause?

### 2026-04-19T11:02:29.071Z · 8896c76e-47b1-481d-8912-6cd1cf17848d

[**Coderabbit authorization failure**](<https://www.cursor.com/agents/bc-6e159965-cb89-4a11-8f12-951939150ba6>)

No. Using the full `orgSlug` in the URL is correct and was never the part this fix targeted.

That query string only drives `withNetworkOrgContext` (resolve the org after you are already authenticated). The CodeRabbit-style failure you described happens **after** OAuth when the **Bearer** JWT is accepted by `jwtVerify` but `withBearerAuth` never attached `user` / `session` because the token lacked `sid`. `requireAuth` then returned 401 regardless of a valid `orgSlug`.

So: a proper `orgSlug` avoids the **400** “missing orgSlug” path; the change in the PR still applies to the **401 after OAuth** path when the access token omits `sid`.

**Repository:** `ctxpipe-ai/ctxpipe`
**Branch:** `cursor/coderabbit-mcp-oauth-bearer-sub-0ba6`

[View PR](<https://github.com/ctxpipe-ai/ctxpipe/pull/113>) · [Open in Desktop](<https://cursor.com/background-agent?bcId=bc-6e159965-cb89-4a11-8f12-951939150ba6>) · [Open in Web](<https://www.cursor.com/agents/bc-6e159965-cb89-4a11-8f12-951939150ba6>)

### 2026-04-19T11:01:22.593Z · 4b501e62-2fb1-40f2-b918-10505ad31010

I use the full org slug in the URL:[ `https://app.ctxpipe.ai/mcp?orgSlug=trurecai-wc4`](<https://app.ctxpipe.ai/mcp?orgSlug=trurecai-wc4>) .

Does that change anything in your solution?

### 2026-04-17T12:34:02.319Z · 8896c76e-47b1-481d-8912-6cd1cf17848d

[**Coderabbit authorization failure**](<https://www.cursor.com/agents/bc-6e159965-cb89-4a11-8f12-951939150ba6>)

## What was going wrong

After OAuth, CodeRabbit calls `/mcp` with a **Bearer** access JWT. Our `withBearerAuth` middleware only loaded `user` / `session` when the JWT included a `sid` (session id). If the token only had `sub` (user id) — which some OAuth/MCP stacks do — verification succeeded, we called `next()` **without** setting the user, and `requireAuth` returned **401**. Many hosts turn that into a vague “enter your access token” step, which matches CTX-16.

IDE clients often still send **cookies** or tokens that include `sid`, so they kept working.

## What we changed

In `withBearerAuth`, when `sid` is missing we now resolve the **latest** `sessions` row for `users.id = payload.sub` and attach that session and user. If `sid` is present but not in the database, we return **401** with `invalid_token` instead of silently continuing.

Relevant logic:

```245:289:apps/backend/src/auth/withAuth.ts
  const db = getSystemDb()
  let tokenSessionContext: { session: AuthSession; user: AuthUser } | undefined

  if (tokenSessionId) {
    const tokenSessionRows = await db
      .select({ session: sessions, user: users })
      .from(sessions)
      .innerJoin(users, eq(sessions.userId, users.id))
      .where(eq(sessions.id, tokenSessionId))
      .limit(1)
    tokenSessionContext = tokenSessionRows[0]
  } else {
    // OAuth access tokens from some MCP clients omit `sid` but still carry `sub`
    // (user id). Resolve the latest DB session for that user so `/mcp` can auth.
    const rows = await db
      .select({ session: sessions, user: users })
      .from(sessions)
      .innerJoin(users, eq(sessions.userId, users.id))
      .where(eq(users.id, payload.sub))
      .orderBy(desc(sessions.updatedAt))
      .limit(1)
    tokenSessionContext = rows[0]
  }

  if (tokenSessionContext) {
    c.set("session", tokenSessionContext.session)
    c.set("user", tokenSessionContext.user)
    return next()
  }

  logBearerAuthFailure(
    new Error(
      tokenSessionId
        ? "Bearer JWT session id not found"
        : "Bearer JWT subject has no resolvable session",
    ),
    { kid },
  )
  return c.json(
    { error: "Unauthorized" },
    401,
    wwwAuthenticateInvalidToken("The access token could not be validated"),
  )
```

## Your screenshot — separate trap

The form shows `orgSlug=` with **no value**. Our `/mcp` route returns **400** when `orgSlug` is missing, before auth. CodeRabbit must use a full URL like `https://app.ctxpipe.ai/mcp?orgSlug=your-org-slug`. Worth confirming in the product UI so people do not paste a half-filled template.

## Delivery

* Branch: `cursor/coderabbit-mcp-oauth-bearer-sub-0ba6`
* PR: [https://github.com/ctxpipe-ai/ctxpipe/pull/113](<https://github.com/ctxpipe-ai/ctxpipe/pull/113>)
* Tests run: `vitest run src/auth/withAuth.test.ts` and `src/routes/mcp.test.ts` (full backend suite still needs Postgres for several suites in this environment).

**Assumption to validate in staging:** Better Auth’s access JWT `sub` is always the same as `users.id`. If a deployment ever used a different subject format, we would need an extra mapping step — worth a quick check against a real token from production once this is deployed.

**Repository:** `ctxpipe-ai/ctxpipe`
**Branch:** `cursor/coderabbit-mcp-oauth-bearer-sub-0ba6`

[View PR](<https://github.com/ctxpipe-ai/ctxpipe/pull/113>) · [Open in Desktop](<https://cursor.com/background-agent?bcId=bc-6e159965-cb89-4a11-8f12-951939150ba6>) · [Open in Web](<https://www.cursor.com/agents/bc-6e159965-cb89-4a11-8f12-951939150ba6>)

### 2026-04-17T12:21:23.585Z

This thread is for an agent session with Cursor. [View on Cursor →](https://www.cursor.com/agents/bc-6e159965-cb89-4a11-8f12-951939150ba6)
