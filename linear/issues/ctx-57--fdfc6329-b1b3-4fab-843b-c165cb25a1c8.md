---
source: linear
type: issue
id: fdfc6329-b1b3-4fab-843b-c165cb25a1c8
identifier: CTX-57
title: Batch / multiple raise MCP install PRs doesn't work
url: https://linear.app/ctxpipe/issue/CTX-57/batch-multiple-raise-mcp-install-prs-doesnt-work
state: Done
priority: Urgent
teamId: 080a5a17-fa66-45af-9009-35d46eed1fc9
projectId: 29b1bae6-2c19-4e9f-b8b0-4a7a2bd28b3e
cycleId: null
assigneeId: bf21aa8a-4c82-447c-9af7-94b0e739ec50
creatorId: 4b501e62-2fb1-40f2-b918-10505ad31010
labelIds:
  - 81bb09c4-a8eb-41b2-ac0a-bd2168a227d4
createdAt: 2026-04-16T00:46:51.976Z
updatedAt: 2026-04-21T05:09:42.612Z
githubReferences:
  - kind: pull_request
    url: https://github.com/ctxpipe-ai/ctxpipe/pull/107
    title: "fix(github): harden batch MCP config PR branch creation (CTX-57)"
attachments: []
---

# CTX-57: Batch / multiple raise MCP install PRs doesn't work

There is a bug when trying to install the MCP into multiple repos via the 'Install MCP via PRs' modal.

In TruRecAI's org, when I try and raise PRs for multiple repos at the same time, it errors out after a while.
In the UI:Reference update failed - [https://docs.github.com/rest/git/refs#create-a-reference](<https://docs.github.com/rest/git/refs#create-a-reference>)

`Failed to load resource: net::ERR_CONNECTION_REFUSEDUnderstand this error `

Browser console logs below:

```
mcp-config-prs:1  Failed to load resource: the server responded with a status of 500 ()Understand this error
McpConfigPrWizard-CfxsbFfJ.js:6 Uncaught (in promise) Error: Reference update failed - https://docs.github.com/rest/git/refs#create-a-reference at Object.mutationFn (McpConfigPrWizard-CfxsbFfJ.js:6:3748) mutationFn @ McpConfigPrWizard-CfxsbFfJ.js:6Understand this error
```

There has been a previous attempt to fix this issue, so review previous work and step through carefully and find a clean and simple approach to this issue. 

If time/latency is an issue, then we can improve the messaging and feedback on the submit PR button, so user's are aware on potential batch PR delays.

## Comments

### 2026-04-16T11:53:13.068Z · 8896c76e-47b1-481d-8912-6cd1cf17848d

[**Batch MCP install PRs**](<https://www.cursor.com/agents/bc-000651d2-0122-4609-b5ff-d4729ed78d12>)

Here is what changed.

**Removed** `MCP_STREAM_BASE_URL` entirely — it is gone from `apps/backend/src/config/env.ts`, `docker-compose.yml`, `docker-compose.env.example`, `apps/backend/.env.example`, and from `github-mcp-config-pr.ts`. MCP URLs in PRs and preview again use `AUTH_BASE_URL` only (trailing slash stripped), same as before that variable existed.

**UI** — Dropped the whole “Remote MCP URL used in generated files: …” block and the unused `mcpStreamUrlForOrg` / `getPublicAppOriginForMcpPreview` wiring from `McpConfigPrWizard.tsx`. The “We read each path on your default branch…” line stays.

**Unchanged** — The GitHub-side hardening remains: `git.getRef` for the default branch tip SHA before `createRef`, plus retries on “Reference update failed”.

Pushed on `cursor/mcp-batch-pr-retry-8d12` (commit `5f1b451`).

**Repository:** `ctxpipe-ai/ctxpipe`
**Branch:** `cursor/mcp-batch-pr-retry-8d12`

[View PR](<https://github.com/ctxpipe-ai/ctxpipe/pull/107>) · [Open in Desktop](<https://cursor.com/background-agent?bcId=bc-000651d2-0122-4609-b5ff-d4729ed78d12>) · [Open in Web](<https://www.cursor.com/agents/bc-000651d2-0122-4609-b5ff-d4729ed78d12>)

### 2026-04-16T11:50:55.055Z · 4b501e62-2fb1-40f2-b918-10505ad31010

The MCP_stream_base_URL shouldn't exist in the first place. Adjust the solution accodingly - this is getting over complicated.

Remove the 'Remote MCP URL used in generated files: XYZ' text altogether in the UI. It's redundant.

### 2026-04-16T11:42:59.456Z · 8896c76e-47b1-481d-8912-6cd1cf17848d

[**Batch MCP install PRs**](<https://www.cursor.com/agents/bc-000651d2-0122-4609-b5ff-d4729ed78d12>)

Here is a tighter read of what was going on and what we changed.

## On “422 = wrong payload”

GitHub’s own docs for [Create a reference](<https://docs.github.com/en/rest/git/refs?apiVersion=2022-11-28#create-a-reference>) say **422** means *“Validation failed, or the endpoint has been spammed”* — so it is **not only** “malformed JSON”. A very common case is `sha` not being the current tip of the branch you are branching from (GitHub still returns **422**, often with “Reference update failed” in the message). So treating every 422 as “our POST body is invalid” is too narrow.

That said, we **did** harden the **Git** side of the payload:

* We now resolve the default branch tip with `GET /repos/{owner}/{repo}/git/ref/heads/{defaultBranch}` (`octokit.rest.git.getRef`) and use `object.sha` as the `sha` for `POST .../git/refs`, instead of going via `repos.getBranch` → `commit.sha`. That keeps the `sha` aligned with the same ref namespace `createRef` validates against.
* The existing **retry + backoff** on “Reference update failed” stays, for races where the tip moves between read and create.

## On `localhost:3000` in the modal / PR JSON

That was not the **POST body to our API** being wrong; it was the **MCP URL we embed** in generated JSON:

* The backend used `AUTH_BASE_URL` for `…/mcp?orgSlug=…`. In many deployments `AUTH_BASE_URL` is internal (e.g. `http://localhost:3000` inside Docker) while `CTXPIPE_PUBLIC_APP_URL` is what you already use for the UI build — so PRs and previews showed localhost.

**Fixes:**

1. `MCP_STREAM_BASE_URL` in `apps/backend/src/config/env.ts` (optional URL). `mcpStreamBaseUrlFromEnv(env)` uses it when set, otherwise `AUTH_BASE_URL`.
2. `docker-compose.yml` (backend service):  
   `MCP_STREAM_BASE_URL: ${CTXPIPE_PUBLIC_APP_URL:-}`  
   So when you set `CTXPIPE_PUBLIC_APP_URL` for the UI image, the backend gets the **same public origin** for MCP URLs without duplicating config under a new root env name.
3. `McpConfigPrWizard`: for the **on-screen** “Remote MCP URL” line, the preview now prefers `window.location.origin` in the browser so it tracks whatever host you are actually using (production vs dev), then falls back to `VITE_PUBLIC_API_URL`, then `https://app.ctxpipe.ai`.

Docs/comments were updated in `apps/backend/.env.example` and `docker-compose.env.example`.

## Tests

* `pnpm exec vitest run src/models/github-mcp-config-pr.test.ts` (includes `mcpStreamBaseUrlFromEnv` preferring `MCP_STREAM_BASE_URL`).

All of this is pushed on `cursor/mcp-batch-pr-retry-8d12` (same branch as PR **#107**). I did **not** rewrite the PR description in GitHub so any edits you made there stay intact.

**Deploy note:** ensure production has either `MCP_STREAM_BASE_URL` set explicitly **or** (with Compose) `CTXPIPE_PUBLIC_APP_URL` set so the backend receives the public origin; otherwise it still falls back to `AUTH_BASE_URL`.

**Repository:** `ctxpipe-ai/ctxpipe`
**Branch:** `cursor/mcp-batch-pr-retry-8d12`

[View PR](<https://github.com/ctxpipe-ai/ctxpipe/pull/107>) · [Open in Desktop](<https://cursor.com/background-agent?bcId=bc-000651d2-0122-4609-b5ff-d4729ed78d12>) · [Open in Web](<https://www.cursor.com/agents/bc-000651d2-0122-4609-b5ff-d4729ed78d12>)

### 2026-04-16T11:26:32.128Z · 4b501e62-2fb1-40f2-b918-10505ad31010

Revisit this.

The error we get is 422, unprocessable entity - which means the payload we're sending to Github is wrong. Verify this and fix.

![CleanShot 2026-04-16 at 21.24.48.png](https://uploads.linear.app/59dca29b-5862-4ade-bd5e-78a7e57013d5/2dac19ec-e80a-44fd-b0a0-75aad79e665f/39f4fc15-5177-4745-b1e2-8b17dd7d87cc)

In addition, there's some text in the modal which has 'localhost:3000' as the URL of the MCP - which is wrong - it needs to be for production.

It might be we're sending the wrong body via the POST.

Docs may help?

![CleanShot 2026-04-16 at 21.26.02.png](https://uploads.linear.app/59dca29b-5862-4ade-bd5e-78a7e57013d5/0e56cf31-0c6f-4b8b-a84e-687b0b7c4868/327fe1cb-92d1-4757-9b57-cc7ccd80b222)

### 2026-04-16T01:07:55.225Z · 4b501e62-2fb1-40f2-b918-10505ad31010

@jakub can you give some guidance on this one and see if Cursor is on the right track please?

### 2026-04-16T01:02:43.587Z · 8896c76e-47b1-481d-8912-6cd1cf17848d

[**Batch MCP install PRs**](<https://www.cursor.com/agents/bc-000651d2-0122-4609-b5ff-d4729ed78d12>)

## What was going wrong

The error string **"Reference update failed"** with the link to [create a reference](<https://docs.github.com/rest/git/refs#create-a-reference>) is GitHub’s **422** when `createRef` is given a `sha` that is **no longer the tip** of the default branch. With **many repos in one HTTP request**, each repo does several API calls; the default branch can move (other pushes, merges, automation) **after** we read `getBranch` but **before** `git.createRef`. That is a race, not (primarily) duplicate branch names — the repo already had per-repo random branch names.

The `net::ERR_CONNECTION_REFUSED` line in the console is usually **something else** (another request, dev proxy, or the tab losing the server). The **500** on `mcp-config-prs` is the backend returning the GitHub error message from our catch block.

## What we changed

* `createCtxpipeMcpConfigPullRequests` now creates the head ref via `createHeadRefFromDefaultBranch`: on **422** + “reference update failed”, it **waits briefly**, **re-reads** `repos.get` + `repos.getBranch`, and **retries** `createRef` (up to 6 attempts). If the ref name already exists (unlikely), it picks a **new** branch name (up to 4 attempts).
* **UI**: when **more than one** repo is selected, a line under the buttons notes that **batch PRs can take a while** because we call GitHub per repo.
* **Tests**: small unit tests for the Octokit **422** message detection so we do not regress on the string GitHub returns.

Draft PR: [**https://github.com/ctxpipe-ai/ctxpipe/pull/107**](<https://github.com/ctxpipe-ai/ctxpipe/pull/107>) (branch `cursor/mcp-batch-pr-retry-8d12`).

If you still see **connection refused** on the main API URL after this ships, that is worth treating as **infra / URL / proxy** rather than GitHub ref logic — happy to trace that separately if you can capture which exact URL fails in the Network tab.

**Repository:** `ctxpipe-ai/ctxpipe`
**Branch:** `cursor/mcp-batch-pr-retry-8d12`

[View PR](<https://github.com/ctxpipe-ai/ctxpipe/pull/107>) · [Open in Desktop](<https://cursor.com/background-agent?bcId=bc-000651d2-0122-4609-b5ff-d4729ed78d12>) · [Open in Web](<https://www.cursor.com/agents/bc-000651d2-0122-4609-b5ff-d4729ed78d12>)

### 2026-04-16T00:46:52.092Z

This thread is for an agent session with Cursor. [View on Cursor →](https://www.cursor.com/agents/bc-000651d2-0122-4609-b5ff-d4729ed78d12)
